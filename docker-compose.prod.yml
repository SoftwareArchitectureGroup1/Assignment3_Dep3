services:
  backend:
    build:
      context: backend/
      dockerfile: ./Dockerfile
      target: prod
    container_name: backend
    networks:
      - backend
    ports:
      - 8000:8000
networks:
  backend:
    name: backend_network
